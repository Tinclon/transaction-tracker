"use strict";

import fs_extra from "fs-extra";
const { readdirSync, lstatSync, existsSync, mkdirSync, removeSync, copySync, lutimesSync } = fs_extra;

const vendorToCategoryMap = {};

(() => {
    // Read in the current statements
    // Classify them
    // Output the totals
})();
